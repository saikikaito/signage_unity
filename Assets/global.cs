using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class global : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		BgmManager.Instance.Play("bgm2");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
